name: string-analyzer-service
runtime: python3.11

build:
  commands:
    - pip install -r requirements_leapcell.txt

run:
  command: uvicorn main_leapcell:app --host 0.0.0.0 --port $PORT

env:
  PORT: 8000

health_check:
  path: /
  port: 8000